/**
 * Camera module barrel.
 *
 * Exports a single CameraRig that wraps a THREE.PerspectiveCamera and
 * provides cockpit / chase / external / tower / free modes.
 */

export { CameraRig } from './camera-rig.js';
export type { CameraMode } from './camera-rig.js';
